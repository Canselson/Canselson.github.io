# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

node_modules
dist
dist-ssr
*.local

# Editor directories and files
.vscode/*
!.vscode/extensions.json
.idea
.DS_Store
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?

# One-off Supabase migration SQL — run manually, kept locally for reference,
# not committed (no secrets in these, just schema/RLS, but keep DB setup out
# of the public repo on principle).
*.sql
